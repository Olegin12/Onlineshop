function addDataToJson () {
    let user = {
        name: "admin",
        email: "admin@dmin.com",
        password: "adminadmin",
    }
    console.log(JSON.stringify(user));
}

export default addDataToJson();